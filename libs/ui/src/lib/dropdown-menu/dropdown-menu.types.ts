/**
 * Where the popup opens relative to the trigger.
 * Logical (start/end) — flips correctly under `dir="rtl"`.
 */
export type DropdownPlacement = 'bottom-start' | 'bottom-end' | 'top-start' | 'top-end';
